# Звіт до лабораторної роботи №1

## 1. Тема, номер і варіант

**Лабораторна робота №1**

**Тема:** «Розгортання Java-проєкту та базова обробка даних».

**Дисципліна:** «Кросплатформні засоби програмування».

**Варіант:** №9 — «Комунальні показники».

**Операційна система:** Windows.

**Версія Java:** Java 21.

**GitHub-репозиторій:** `kzp-lab01-Kolodij`.

Лабораторна робота виконується в окремій Git-гілці:

`lab01`

---

## 2. Мета роботи

Мета роботи — створити початкову версію консольної Java-програми,
яка читає текстові дані предметної області, перевіряє вхідні записи,
виконує базові числові обчислення та формує текстовий звіт.

У межах першого рівня реалізовано:

- базову структуру Java-проєкту;
- запуск програми з командного рядка;
- підтримку аргументу `--help`;
- підтримку аргументів `--input` і `--output`;
- читання CSV-файла у кодуванні UTF-8;
- перевірку структури кожного запису;
- перевірку обов’язкових текстових полів;
- перетворення числових значень у тип `double`;
- обробку нечислових і від’ємних значень;
- продовження роботи після помилки в окремому рядку;
- обчислення показників варіанта №9;
- формування текстового звіту;
- виведення звіту в консоль;
- запис звіту у текстовий файл.

---

## 3. Постановка задачі

Предметна область варіанта №9 — **«Комунальні показники»**.

Один запис має формат:

`meter;date;value;tariff`

Типи полів:

- `meter` — `String`;
- `date` — `String`;
- `value` — `double`;
- `tariff` — `double`.

Призначення полів:

- `meter` — назва або ідентифікатор лічильника;
- `date` — дата показника;
- `value` — значення споживання;
- `tariff` — тариф.

Приклад коректного запису:

`Електролічильник-1;2026-09-01;125.50;4.32`

Для всіх коректних записів потрібно визначити:

1. кількість коректних записів;
2. сумарне споживання;
3. загальну вартість;
4. найбільше споживання.

Загальна вартість визначається як сума добутків:

`value * tariff`

для всіх коректних записів.

Некоректний рядок не повинен аварійно завершувати виконання програми.
Для пошкодженого запису програма повідомляє номер рядка та причину
його пропуску, після чого переходить до наступного запису.

---

## 4. Структура програми

Після переходу до другого рівня проєкт має Maven-структуру:

```text
kzp-labs-Kolodij/
├── .mvn/
│   └── wrapper/
├── ai/
│   ├── manager.md
│   └── devops.md
├── data/
│   └── input.csv
├── src/
│   ├── main/
│   │   └── java/
│   │       └── ua/
│   │           └── lpnu/
│   │               └── kzp/
│   │                   └── Main.java
│   └── test/
│       └── java/
│           └── ua/
│               └── lpnu/
│                   └── kzp/
│                       └── MainTest.java
├── .editorconfig
├── .gitattributes
├── .gitignore
├── mvnw
├── mvnw.cmd
├── pom.xml
├── README.md
└── REPORT.md
```

Головний клас програми:

`ua.lpnu.kzp.Main`

Точка входу:

`public static void main(String[] args)`

Тестовий клас:

`ua.lpnu.kzp.MainTest`

Після реалізації основної функціональності було виконано
рефакторинг `Main.java`.

Логіку програми розділено на окремі допоміжні методи:

- `printHelp()` — виведення довідки;
- `readInput()` — читання вхідного файла;
- `processLines()` — перевірка записів та обчислення показників;
- `formatReport()` — формування текстового звіту;
- `writeReport()` — запис звіту у файл.

Загальний потік роботи програми:

```text
аргументи командного рядка
        ↓
визначення input/output
        ↓
читання CSV у UTF-8
        ↓
перевірка кожного рядка
        ↓
перетворення числових полів
        ↓
обчислення показників
        ↓
формування звіту
        ↓
виведення в консоль
        ↓
запис у текстовий файл
```

Для побудови шляхів використовується клас `Path`.

Наприклад:

`Path.of("data", "input.csv")`

Для читання файла використовується:

`Files.readAllLines`

із явним кодуванням:

`StandardCharsets.UTF_8`

CSV-рядок розділяється командою:

`line.split(";", -1)`

Параметр `-1` дозволяє зберігати порожні поля.

Числові значення перетворюються через:

`Double.parseDouble`

Якщо значення неможливо перетворити на число,
виникає `NumberFormatException`.

Помилка обробляється для конкретного рядка,
тому один некоректний запис не завершує роботу всієї програми.

Для кожного запису використовується послідовність:

`прочитати → перевірити → перетворити → включити в обчислення`

Некоректні записи не впливають на підсумкові показники.

Рефакторинг не змінив функціональну поведінку програми,
що було перевірено автоматизованими тестами.

---

## 5. Інфраструктура

Для виконання лабораторної роботи використовується Java 21.

Версію Java було перевірено командами:

```powershell
java -version
javac -version
```

Для керування проєктом на другому рівні підключено Maven.

Основна конфігурація Maven знаходиться у файлі:

`pom.xml`

У ньому налаштовано:

- Java 21;
- кодування UTF-8;
- JUnit 5;
- SpotBugs;
- Maven Shade Plugin.

Для проєкту також створено Maven Wrapper.

Основні файли Wrapper:

```text
.mvn/
mvnw
mvnw.cmd
```

Завдяки Maven Wrapper команди Maven можна виконувати
без залежності від конкретної глобально встановленої версії Maven.

На Windows використовується команда:

```powershell
.\mvnw.cmd
```

### Компіляція

```powershell
.\mvnw.cmd compile
```

Компіляція завершується результатом:

```text
BUILD SUCCESS
```

### JUnit 5

Для автоматизованого тестування підключено JUnit 5.

У `pom.xml` використовується залежність:

```text
org.junit.jupiter:junit-jupiter:5.12.2
```

Тести запускаються командою:

```powershell
.\mvnw.cmd test
```

### SpotBugs

Для статичного аналізу коду використовується:

`spotbugs-maven-plugin`

Версія:

`4.9.7.0`

SpotBugs прив’язано до Maven-фази:

`verify`

і використовується ціль:

`check`

Запуск:

```powershell
.\mvnw.cmd verify
```

Під час перевірки було отримано:

```text
BugInstance size is 0
Error size is 0
No errors/warnings found
BUILD SUCCESS
```

### Maven Shade Plugin

Для створення виконуваного JAR підключено:

`maven-shade-plugin`

Версія:

`3.6.0`

Плагін прив’язано до Maven-фази:

`package`

Головним класом JAR визначено:

`ua.lpnu.kzp.Main`

Збірка виконується командою:

```powershell
.\mvnw.cmd package
```

Після успішної збірки створюється:

`target/lab01-1.0.0.jar`

Файл можна запускати командою:

```powershell
java -jar target\lab01-1.0.0.jar
```

### `.gitignore`

У `.gitignore` виключено з Git файли,
які можуть бути автоматично створені повторно:

```text
target/
out/
reports/
.idea/
*.iml
.vscode/
```

Каталоги `target/`, `out/` та `reports/`
містять результати збірки або роботи програми,
тому вони не повинні зберігатися у репозиторії.

### `.gitattributes`

Використовується правило:

```text
* text=auto eol=lf
```

Це забезпечує однакове зберігання завершень рядків у Git.

### `.editorconfig`

Файл задає:

- UTF-8;
- завершення рядків LF;
- відступ пробілами;
- розмір відступу 4 пробіли;
- завершальний перенос рядка.

---

## 6. GitHub Issues і Pull Request

Для лабораторної роботи використовується публічний GitHub-репозиторій:

`kzp-labs-Kolodij`

Початкова структура проєкту була зафіксована комітом:

`chore: initialize lab01 project structure`

Для виконання лабораторної роботи створено окрему гілку:

`lab01`

Рівні лабораторної роботи виконуються послідовно
в цій гілці.

Для організації роботи створено GitHub Issues.

### Issue #1

`Налаштування лабораторної роботи №1`

Issue описує початкове налаштування проєкту:

- створення структури;
- налаштування Java;
- створення службових файлів;
- реалізацію базової програми;
- реалізацію `--help`, `--input` та `--output`;
- створення `README.md` і `REPORT.md`.

### Issue #2

`Налаштувати Maven-проєкт`

У межах Issue виконано:

- створення `pom.xml`;
- налаштування Java 21 та UTF-8;
- створення Maven Wrapper;
- підключення JUnit 5;
- створення автоматизованих тестів;
- успішний запуск `mvn test`.

### Issue #3

`Налаштувати аналізатор і пакування`

У межах Issue виконано:

- підключення SpotBugs;
- запуск SpotBugs у фазі `verify`;
- успішний статичний аналіз;
- підключення Maven Shade Plugin;
- створення виконуваного JAR;
- перевірку запуску JAR.

### Issue #4

`Налаштувати GitHub Actions`

Issue створено для наступного, третього рівня лабораторної роботи.

На другому рівні це завдання ще не виконується.

Issues #1, #2 та #3 залишаються відкритими до завершення
лабораторної роботи.

Після завершення всіх рівнів буде створено Pull Request:

`lab01 → main`

У Pull Request планується використати:

```text
Closes #1
Closes #2
Closes #3
Closes #4
```

Після code review та злиття Pull Request у `main`
відповідні Issues будуть закриті автоматично.

На момент виконання другого рівня Pull Request ще не зливається,
оскільки робота в гілці `lab01` продовжується на третьому рівні.

---

## 7. Приклади роботи

### Вхідний файл

Файл:

`data/input.csv`

містить:

```text
Електролічильник-1;2026-09-01;125.50;4.32
Водомір-1;2026-09-02;12.75;35.20
Електролічильник-2;2026-09-03;48.00;4.32
Теплолічильник-1;2026-09-04;-10.00;8.50
Водомір-2;2026-09-05;abc;35.20
Електролічильник-3;;90.00;4.32
Газовий-лічильник;2026-09-07;33.00
```

Перші три записи є коректними.

Четвертий запис містить від’ємне значення `value`.

П’ятий запис містить текст `abc` замість числа.

Шостий запис містить порожнє поле `date`.

Сьомий запис містить три поля замість чотирьох.

### Створення JAR

```powershell
.\mvnw.cmd package
```

Після успішної збірки:

```text
BUILD SUCCESS
```

створюється:

`target/lab01-1.0.0.jar`

### Звичайний запуск

```powershell
java -jar target\lab01-1.0.0.jar
```

Приклад результату:

```text
Прочитано рядків: 7
Рядок 1: запис коректний
Рядок 2: запис коректний
Рядок 3: запис коректний
Рядок 4 пропущено: value не може бути від'ємним
Рядок 5 пропущено: value і tariff мають бути числами
Рядок 6 пропущено: поле date не може бути порожнім
Рядок 7 пропущено: очікується 4 поля

Результати:
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50
```

За замовчуванням звіт записується у:

`out/report.txt`

### Параметр `--help`

```powershell
java -jar target\lab01-1.0.0.jar --help
```

Результат:

```text
Використання: java -jar <файл.jar> [--help] [--input <файл>] [--output <файл>]
```

### Параметр `--input`

```powershell
java -jar target\lab01-1.0.0.jar --input data\input.csv
```

Параметр `--input` дозволяє передати шлях
до вхідного файла без зміни вихідного коду.

### Параметр `--output`

```powershell
java -jar target\lab01-1.0.0.jar --output reports\result.txt
```

Параметр `--output` дозволяє визначити шлях
до вихідного файла звіту.

### Одночасне використання параметрів

```powershell
java -jar target\lab01-1.0.0.jar --input data\input.csv --output reports\result.txt
```

Створений файл містить:

```text
Результати:
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50
```

Файл записується у UTF-8.

Для форматування числових значень використовується:

`Locale.ROOT`

тому десятковим роздільником залишається крапка.

---

## 8. Тестування

На першому рівні програма перевірялася вручну.

На другому рівні було додано автоматизовані тести
за допомогою JUnit 5.

Тестовий клас:

`src/test/java/ua/lpnu/kzp/MainTest.java`

Для тимчасових тестових файлів використовується:

`@TempDir`

Завдяки цьому тести не залежать від постійних файлів проєкту.

### Тест `processesValidAndInvalidRecords()`

Тест перевіряє одночасну обробку:

- коректних записів;
- від’ємного значення;
- нечислового значення;
- українських символів.

Після виконання перевіряється сформований звіт.

Очікувані значення:

```text
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50
```

### Тест `ignoresMalformedRecords()`

Тест перевіряє:

- порожній рядок;
- порожнє поле `date`;
- неправильну кількість полів;
- коректний запис серед помилкових даних.

Некоректні записи повинні бути пропущені,
а коректний запис повинен потрапити в підсумкові обчислення.

### Тест `doesNotCreateReportWhenNoValidRecordsExist()`

Тест використовує файл,
у якому немає жодного коректного запису.

Перевіряється, що програма:

- не формує некоректну статистику;
- повідомляє про відсутність коректних записів;
- не створює вихідний файл звіту.

### Запуск тестів

```powershell
.\mvnw.cmd test
```

Результат:

```text
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Після рефакторингу `Main.java` всі три тести
були запущені повторно.

Результат залишився успішним,
що підтвердило відсутність зміни поведінки програми.

### Статичний аналіз

Після тестів виконано:

```powershell
.\mvnw.cmd verify
```

SpotBugs повідомив:

```text
BugInstance size is 0
Error size is 0
No errors/warnings found
BUILD SUCCESS
```

### Перевірка пакування

Виконано:

```powershell
.\mvnw.cmd package
```

Результат:

```text
BUILD SUCCESS
```

Після цього перевірено запуск:

```powershell
java -jar target\lab01-1.0.0.jar --help
```

а також:

```powershell
java -jar target\lab01-1.0.0.jar --input data\input.csv
```

Усі перевірки виконані успішно.

---

## 9. Документація

Для основного класу програми використовується Javadoc.

Документовано:

- клас `Main`;
- приватний конструктор `Main()`;
- метод `main(String[] args)`;
- метод `printHelp()`;
- метод `readInput()`;
- метод `processLines()`;
- метод `formatReport()`;
- метод `writeReport()`.

Для методів описано їх призначення,
параметри, результати та можливі винятки.

У `README.md` описано:

- номер лабораторної роботи;
- номер варіанта;
- предметну область;
- призначення програми;
- формат CSV;
- перевірку вхідних даних;
- перелік обчислюваних показників;
- структуру Maven-проєкту;
- Maven Wrapper;
- компіляцію;
- JUnit 5;
- SpotBugs;
- Maven Shade Plugin;
- створення виконуваного JAR;
- запуск через `java -jar`;
- `--help`;
- `--input`;
- `--output`;
- рекомендовану послідовність перевірки проєкту;
- використання GitHub Issues;
- використання ШІ.

Основні команди для локальної перевірки
також задокументовано:

```powershell
.\mvnw.cmd test
.\mvnw.cmd verify
.\mvnw.cmd package
```

Документація відповідає фактичному стану проєкту
після завершення другого рівня.

---

## 10. Академічна доброчесність

Під час виконання лабораторної роботи використовувався
генеративний інструмент **ChatGPT від OpenAI**.

ChatGPT використовувався як допоміжний інструмент для:

- пояснення методичних вимог;
- планування послідовності виконання роботи;
- пояснення команд Git і GitHub;
- пояснення конструкцій Java;
- аналізу структури коду;
- пояснення Maven;
- налаштування Maven Wrapper;
- пояснення JUnit 5;
- підготовки автоматизованих тестів;
- пояснення SpotBugs;
- пояснення Maven Shade Plugin;
- пояснення процесу створення виконуваного JAR;
- допомоги під час рефакторингу `Main.java`;
- перевірки відповідності реалізації вимогам поточного рівня;
- оновлення технічної документації.

Усі команди виконувалися автором роботи.

Програмний код був введений,
запущений та перевірений автором роботи.

Автор роботи може пояснити призначення використаних конструкцій,
логіку програми, конфігурацію Maven,
роботу тестів, статичного аналізатора та процес пакування.

### Роль Manager

У файлі:

`ai/manager.md`

описана роль Manager.

Її призначення:

- аналізувати вимоги методичних вказівок;
- розбивати роботу на послідовні етапи;
- контролювати виконання вимог поточного рівня;
- не переходити до наступного рівня до завершення попереднього;
- допомагати перевіряти критерії готовності.

Приклад промпту:

> Працюй лише за методичними вказівками.
> Розбий вимоги на послідовні кроки,
> пояснюй мету кожного кроку
> та перевіряй критерії готовності.

### Роль DevOps

У файлі:

`ai/devops.md`

описана роль DevOps.

Її призначення:

- допомога з Git і GitHub;
- Maven;
- Maven Wrapper;
- автоматизоване тестування;
- статичний аналіз;
- створення JAR;
- GitHub Actions.

На другому рівні роль DevOps використовувалася
для Maven, Maven Wrapper, SpotBugs та пакування JAR.

GitHub Actions буде налаштовано на третьому рівні.

Приклад промпту:

> Пояснюй кожен інфраструктурний крок
> та не додавай інструменти,
> які ще не потрібні для поточного рівня.

### Прийняті рекомендації

Під час роботи було прийнято рекомендації:

- виконувати лабораторну роботу у гілці `lab1`;
- використовувати `Path` для шляхів;
- використовувати явне UTF-8;
- використовувати `Locale.ROOT`;
- використовувати `split(";", -1)`;
- обробляти помилку одного рядка без завершення всієї програми;
- відхиляти від’ємні значення;
- перевіряти наявність хоча б одного коректного запису;
- реалізувати `--help`, `--input` та `--output`;
- один раз формувати текст звіту та використовувати його
  для консолі й файла;
- перейти від ручної компіляції до Maven;
- використовувати Maven Wrapper;
- додати JUnit 5;
- додати автоматизовані тести;
- додати SpotBugs до фази `verify`;
- створювати виконуваний JAR через Maven Shade Plugin;
- розділити великий метод `main` на допоміжні методи;
- після рефакторингу повторно запускати тести,
  статичний аналіз та пакування.

### Виправлені помилки та проблеми

Під час початкової розробки було виявлено
помилку компіляції, пов’язану з:

`Locale.ROOT`

Причиною був відсутній імпорт:

```java
import java.util.Locale;
```

Після додавання імпорту програма успішно скомпілювалася.

Також було виявлено дублювання перевірки аргументу:

`--help`

Один із повторних блоків було видалено,
а обробку аргументів залишено в одному циклі.

Під час перевірки українського тексту у Windows Terminal
спостерігалася проблема з відображенням окремих символів.

Було перевірено створений текстовий файл.

У файлі українські символи зберігалися правильно,
що підтвердило коректне використання UTF-8 у програмі.

Після підключення SpotBugs статичний аналіз завершився без помилок:

```text
BugInstance size is 0
Error size is 0
No errors/warnings found
```

Після рефакторингу всі JUnit-тести,
SpotBugs та Maven-пакування продовжили успішно працювати,
тому зміна структури коду не порушила функціональність програми.

---

## 11. Відповіді на контрольні питання

### 1. Для чого потрібен файл `pom.xml`?

`pom.xml` описує Maven-проєкт: його параметри, версію Java,
залежності та плагіни.

На першому рівні Maven ще не використовується.
`pom.xml` буде додано на наступному рівні.

### 2. Для чого використовується метод `main`?

Метод `main` є точкою входу в консольну Java-програму.

JVM починає виконання програми з:

`public static void main(String[] args)`

### 3. Що містить `String[] args`?

Масив `args` містить аргументи,
які були передані програмі з командного рядка.

Наприклад:

```text
--input data\input.csv
```

буде представлено як:

```text
args[0] = "--input"
args[1] = "data\input.csv"
```

### 4. Для чого потрібен параметр `--help`?

`--help` виводить довідку про доступні параметри запуску програми.

### 5. Для чого потрібен параметр `--input`?

`--input` дозволяє вказати інший шлях до вхідного файла
без зміни вихідного коду.

### 6. Для чого потрібен параметр `--output`?

`--output` дозволяє вибрати шлях до файла,
в який буде записано сформований звіт.

### 7. Чому використовується `Path`?

`Path` дозволяє працювати зі шляхами
незалежно від конкретної операційної системи.

Це кросплатформніший підхід,
ніж ручне складання шляхів через `\` або `/`.

### 8. Навіщо явно задавати `StandardCharsets.UTF_8`?

Явне UTF-8 робить читання та запис тексту
незалежними від стандартного кодування операційної системи.

Це важливо для коректної роботи українських символів.

### 9. Для чого використовується `split(";", -1)`?

Метод `split` розділяє CSV-рядок на окремі поля.

Аргумент `-1` дозволяє зберігати порожні поля,
у тому числі порожнє поле в кінці рядка.

### 10. Що станеться при `Double.parseDouble("abc")`?

Виникне виняток:

`NumberFormatException`

У програмі ця помилка перехоплюється,
рядок пропускається,
а обробка наступних записів продовжується.

### 11. Чому неправильний рядок не можна мовчки пропускати?

Користувач повинен знати номер пошкодженого рядка
та причину помилки,
щоб мати можливість виправити вхідні дані.

### 12. Які перевірки виконуються для варіанта №9?

Програма перевіряє:

- чи рядок не порожній;
- чи є рівно 4 поля;
- чи `meter` не порожній;
- чи `date` не порожня;
- чи `value` є числом;
- чи `tariff` є числом;
- чи `value` не від’ємне;
- чи `tariff` не від’ємний;
- чи існує хоча б один коректний запис.

### 13. Чому некоректний запис не повинен змінювати статистику?

Якщо хибне значення додати до суми або врахувати при пошуку максимуму,
кінцевий звіт стане неправильним.

Тому статистика змінюється тільки після проходження всіх перевірок.

### 14. Для чого використовується `Locale.ROOT`?

`Locale.ROOT` дозволяє форматувати дробові числа
незалежно від регіональних налаштувань системи.

Наприклад:

`186.25`

буде записане з крапкою.

### 15. Для чого використовується `%n`?

`%n` додає правильний для поточної операційної системи
роздільник рядків.

Це кросплатформний спосіб перенесення рядка.

### 16. Для чого потрібен Git?

Git зберігає історію змін проєкту,
дозволяє використовувати гілки,
повертатися до попередніх станів
і об’єднувати зміни.

### 17. Навіщо лабораторна робота виконується в окремій гілці?

Окрема гілка дозволяє виконувати зміни,
не змінюючи стабільний стан `main`.

Після завершення роботу можна перевірити через Pull Request
і лише після code review об’єднати з `main`.

### 18. Для чого потрібен GitHub Issue?

GitHub Issue використовується для опису
окремого завдання, проблеми або етапу роботи.

У ньому можна записати мету,
критерії готовності та стан виконання.

### 19. Для чого потрібен агент DevOps?

DevOps допомагає з Git, Maven, Maven Wrapper,
статичним аналізом, JAR і GitHub Actions.

На першому рівні основна його роль була пов’язана
з налаштуванням Git і структури репозиторію.

### 20. Який рядок програми потребує додаткового пояснення?

Одним із найменш очевидних рядків є:

```java
input = Path.of(args[++i]);
```

Оператор `++i` спочатку збільшує значення індексу `i`.

Якщо поточним аргументом є:

`--input`

то наступним елементом масиву є шлях до файла.

Наприклад:

```text
args[0] = "--input"
args[1] = "data\input.csv"
```

Тому після `++i` програма отримує:

`args[1]`

і створює з нього об’єкт `Path`.

---

## 12. Висновки

У результаті виконання першого рівня лабораторної роботи
було створено консольну Java-програму
для предметної області «Комунальні показники».

Програма працює з варіантом №9
та обробляє записи формату:

`meter;date;value;tariff`

Було реалізовано перевірку вхідних даних,
обробку помилкових рядків
і продовження роботи після локальної помилки.

Для коректних записів програма обчислює:

- кількість коректних записів;
- сумарне споживання;
- загальну вартість;
- найбільше споживання.

Реалізовано параметри:

`--help`

`--input`

`--output`

Вхідні та вихідні файли обробляються у UTF-8.

Для кросплатформної роботи використовуються:

- `Path`;
- `Files`;
- `StandardCharsets.UTF_8`;
- `Locale.ROOT`;
- `%n`.

Створено публічний GitHub-репозиторій
та окрему гілку `lab01`.

Поточна реалізація першого рівня
стане основою для переходу до другого рівня лабораторної роботи,
де буде додано Maven, Maven Wrapper, автоматизовані JUnit-тести,
статичний аналіз та створення виконуваного JAR.