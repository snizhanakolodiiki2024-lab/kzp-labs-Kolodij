Звіт до лабораторної роботи №1

1. Тема, номер роботи та варіант

Тема: «Розгортання Java-проєкту та базова обробка даних».

Дисципліна: «Кросплатформні засоби програмування».

Варіант: №9 — «Комунальні показники».

ОС: Windows.

Java: 21.

Репозиторій: kzp-labs-Kolodij.

Робоча гілка: lab1.

2. Мета роботи

Створити консольну Java-програму для обробки даних свого варіанта та підготувати повний інженерний цикл: Maven, автоматизовані тести, статичний аналіз, виконуваний JAR і CI на трьох операційних системах.

3. Постановка задачі

Формат одного запису:

meter;date;value;tariff

Поля:

meter — назва лічильника;

date — дата;

value — споживання;

tariff — тариф.

Для коректних записів програма обчислює:

кількість коректних записів;

сумарне споживання;

загальну вартість;

найбільше споживання.

Некоректний рядок пропускається, але програма повідомляє номер рядка та причину.

4. Реалізація і структура

Основний клас:

src/main/java/ua/lpnu/kzp/Main.java

Тести:

src/test/java/ua/lpnu/kzp/MainTest.java

Основні допоміжні методи Main:

printHelp() — довідка;

printVersion() — версія програми;

readInput() — читання UTF-8 файла;

processLines() — перевірка записів і обчислення;

formatReport() — формування звіту;

writeReport() — запис звіту.

Для шляхів використовується Path, для текстових файлів — StandardCharsets.UTF_8, для чисел — Locale.ROOT.

5. Інфраструктура

Проєкт використовує Maven і Maven Wrapper.

У pom.xml налаштовано:

Java 21;

UTF-8;

JUnit 5;

SpotBugs;

Maven Shade Plugin;

версію 1.0.0.

Основні команди:

.\mvnw.cmd test
.\mvnw.cmd verify
.\mvnw.cmd package

Виконуваний JAR:

target/lab01-1.0.0.jar

Запуск:

java -jar target\lab01-1.0.0.jar

Версія:

java -jar target\lab01-1.0.0.jar --version

Результат:

Комунальні показники 1.0.0

SpotBugs виконується у фазі verify і не виявив дефектів.

6. GitHub Issues і CI

Для роботи створено Issues:

#1 — налаштування лабораторної роботи №1;

#2 — налаштування Maven-проєкту;

#3 — аналізатор і пакування;

#4 — GitHub Actions.

GitHub Actions налаштовано у:

.github/workflows/ci.yml

CI запускає verify на:

ubuntu-latest;

windows-latest;

macos-latest.

Усі три jobs завершилися успішно. Після перевірки JAR публікується як workflow artifact.

Посилання на фінальний CI run:

<додати після фінального запуску>

Посилання на artifact:

<додати після фінального запуску>

Фінальний Pull Request lab1 → main буде створено після завершення додаткового завдання.

7. Приклад роботи

Фрагмент data/input.csv:

Електролічильник-1;2026-09-01;125.50;4.32
Водомір-1;2026-09-02;12.75;35.20
Електролічильник-2;2026-09-03;48.00;4.32
Теплолічильник-1;2026-09-04;-10.00;8.50
Водомір-2;2026-09-05;abc;35.20
Електролічильник-3;;90.00;4.32
Газовий-лічильник;2026-09-07;33.00

Результат для трьох коректних записів:

Результати:
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50

8. Тестування

Використовується JUnit 5.

Реалізовано 7 тестів:

коректні й некоректні записи;

порожній рядок, порожня дата та неправильна кількість полів;

файл без коректних записів;

--version;

порожній meter;

від’ємний tariff;

невідомий аргумент.

Локальний результат:

Tests run: 7, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS

Після рефакторингу всі тести продовжили проходити.

У GitHub Actions тести успішно виконуються на Ubuntu, Windows і macOS.

9. Документація

Оновлено:

README.md;

REPORT.md;

Javadoc у Main.java.

У ai/ створено ролі:

Manager;

DevOps;

Developer;

Validator;

Documenter;

Reviewer.

10. Академічна доброчесність

Під час роботи використовувався ChatGPT як допоміжний інструмент для:

пояснення методичних вимог;

планування етапів;

пояснення Java, Maven і Git;

аналізу тестів;

налаштування SpotBugs і GitHub Actions;

перевірки документації.

Усі команди, зміни та результати перевірялися автором роботи. Автор може пояснити використані конструкції та логіку програми.

11. Короткі відповіді на контрольні питання

pom.xml описує Maven-проєкт, залежності, Java-версію та плагіни.

main є точкою входу в консольну Java-програму.

String[] args містить аргументи командного рядка.

--help показує доступні параметри.

--input задає шлях до вхідного файла.

--output задає шлях до звіту.

--version показує версію програми.

Path використовується для кросплатформних шляхів.

Явне UTF-8 забезпечує однакове кодування на різних ОС.

split(";", -1) зберігає порожні поля.

Double.parseDouble перетворює текст у double; для неправильного числа виникає NumberFormatException.

Некоректний рядок не змінює статистику.

Locale.ROOT забезпечує крапку як десятковий роздільник.

%n створює платформонезалежний перенос рядка.

Maven Wrapper дозволяє запускати проєкт без залежності від локальної версії Maven.

test запускає тести, package створює JAR, verify виконує повну перевірку з аналізатором.

SpotBugs шукає потенційні дефекти у скомпільованому коді.

GitHub Actions автоматично перевіряє однаковий код на різних ОС.

GitHub Issue описує окреме завдання або дефект.

Pull Request використовується для code review і злиття перевірених змін у main.

12. Висновки

У лабораторній роботі реалізовано консольний Java-застосунок для варіанта №9 «Комунальні показники».

Програма читає UTF-8 файл, перевіряє записи, обчислює чотири показники та формує звіт.

Проєкт використовує Maven Wrapper, JUnit 5, SpotBugs і Maven Shade Plugin. Реалізовано 7 автоматизованих тестів.

GitHub Actions успішно перевіряє проєкт на Ubuntu, Windows і macOS та публікує JAR як artifact.

Версія програми — 1.0.0. Фінальний Git-тег v1.0.0 і Pull Request будуть створені після завершення додаткового завдання.