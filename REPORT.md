# Звіт до лабораторної роботи №1

## 1. Тема, номер і варіант

**Лабораторна робота №1**

**Тема:** «Розгортання Java-проєкту та базова обробка даних».

**Дисципліна:** «Кросплатформні засоби програмування».

**Варіант:** №9 — «Комунальні показники».

**Операційна система:** Windows.

**Версія Java:** Java 21.

**GitHub-репозиторій:** `kzp-lab01-Kolodij`.

Лабораторна робота виконується в окремій Git-гілці:

`lab01`

---

## 2. Мета роботи

Мета роботи — створити початкову версію консольної Java-програми,
яка читає текстові дані предметної області, перевіряє вхідні записи,
виконує базові числові обчислення та формує текстовий звіт.

У межах першого рівня реалізовано:

- базову структуру Java-проєкту;
- запуск програми з командного рядка;
- підтримку аргументу `--help`;
- підтримку аргументів `--input` і `--output`;
- читання CSV-файла у кодуванні UTF-8;
- перевірку структури кожного запису;
- перевірку обов’язкових текстових полів;
- перетворення числових значень у тип `double`;
- обробку нечислових і від’ємних значень;
- продовження роботи після помилки в окремому рядку;
- обчислення показників варіанта №9;
- формування текстового звіту;
- виведення звіту в консоль;
- запис звіту у текстовий файл.

---

## 3. Постановка задачі

Предметна область варіанта №9 — **«Комунальні показники»**.

Один запис має формат:

`meter;date;value;tariff`

Типи полів:

- `meter` — `String`;
- `date` — `String`;
- `value` — `double`;
- `tariff` — `double`.

Призначення полів:

- `meter` — назва або ідентифікатор лічильника;
- `date` — дата показника;
- `value` — значення споживання;
- `tariff` — тариф.

Приклад коректного запису:

`Електролічильник-1;2026-09-01;125.50;4.32`

Для всіх коректних записів потрібно визначити:

1. кількість коректних записів;
2. сумарне споживання;
3. загальну вартість;
4. найбільше споживання.

Загальна вартість визначається як сума добутків:

`value * tariff`

для всіх коректних записів.

Некоректний рядок не повинен аварійно завершувати виконання програми.
Для пошкодженого запису програма повідомляє номер рядка та причину
його пропуску, після чого переходить до наступного запису.

---

## 4. Структура програми

Структура проєкту на першому рівні:

```text
kzp-lab01-Kolodij/
├── ai/
│   ├── manager.md
│   └── devops.md
├── data/
│   └── input.csv
├── src/
│   ├── main/
│   │   └── java/
│   │       └── ua/
│   │           └── lpnu/
│   │               └── kzp/
│   │                   └── Main.java
│   └── test/
│       └── java/
├── .editorconfig
├── .gitattributes
├── .gitignore
├── README.md
└── REPORT.md
```

Головний клас програми:

`ua.lpnu.kzp.Main`

Точка входу:

`public static void main(String[] args)`

Загальний потік обробки даних:

```text
аргументи командного рядка
        ↓
визначення шляхів input/output
        ↓
читання CSV у UTF-8
        ↓
перевірка кожного рядка
        ↓
поділ рядка на поля
        ↓
перевірка meter і date
        ↓
перетворення value і tariff у double
        ↓
перевірка числових значень
        ↓
накопичення статистики
        ↓
формування звіту
        ↓
консоль + текстовий файл
```

Для побудови шляхів використовується клас `Path`.

Наприклад:

`Path.of("data", "input.csv")`

Для читання файла використовується `Files.readAllLines`
з явним кодуванням:

`StandardCharsets.UTF_8`

CSV-рядок розділяється командою:

`line.split(";", -1)`

Другий аргумент `-1` потрібний для збереження порожніх полів.

Числові значення перетворюються через:

`Double.parseDouble`

Якщо значення неможливо перетворити на число,
виникає `NumberFormatException`.

Ця помилка перехоплюється під час обробки конкретного рядка,
тому один пошкоджений запис не зупиняє роботу всієї програми.

Для кожного запису використовується послідовність:

`прочитати → перевірити → перетворити → включити в обчислення`

Некоректні записи не впливають на підсумкові показники.

---

## 5. Інфраструктура

На першому рівні використовується Java 21 без Maven.

Версію Java було перевірено командами:

```powershell
java -version
javac -version
```

Програма компілюється командою:

```powershell
javac -d out src\main\java\ua\lpnu\kzp\Main.java
```

Параметр `-d out` задає каталог для скомпільованих `.class`-файлів.

Звичайний запуск виконується командою:

```powershell
java -cp out ua.lpnu.kzp.Main
```

У проєкті використовуються службові файли.

### `.gitignore`

Файл містить правила для файлів і каталогів,
які не повинні потрапляти до Git-репозиторію.

Використовуються такі правила:

```text
target/
out/
.idea/
*.iml
.vscode/
```

Каталог `out/` містить результати компіляції та згенерований звіт,
тому його можна відтворити й не потрібно зберігати у Git.

### `.gitattributes`

Файл містить:

```text
* text=auto eol=lf
```

Це задає єдиний спосіб зберігання завершень рядків у Git
та допомагає уникати відмінностей між операційними системами.

### `.editorconfig`

Файл задає:

- кодування UTF-8;
- завершення рядків LF;
- використання пробілів;
- відступ у 4 пробіли;
- завершальний перенос рядка.

На першому рівні Maven, JUnit, статичний аналізатор,
Maven Wrapper і виконуваний JAR ще не налаштовуються.

Вони будуть додані під час переходу до наступних рівнів.

---

## 6. GitHub Issues і Pull Request

Для лабораторної роботи створено публічний GitHub-репозиторій:

`kzp-lab01-Kolodij`

Початкова структура проєкту була зафіксована комітом:

`chore: initialize lab01 project structure`

Для виконання лабораторної роботи створено окрему гілку:

`lab01`

Також створено GitHub Issue:

**«Налаштування лабораторної роботи №1»**

Issue використовується для контролю виконання основних етапів роботи.

Перший, другий і третій рівні лабораторної роботи виконуються
послідовно в гілці `lab01`.

Після завершення всієї лабораторної роботи буде виконано:

1. `git push` гілки `lab01`;
2. створення Pull Request;
3. code review;
4. злиття перевірених змін у `main`.

---

## 7. Приклади роботи

### Вхідний файл

Файл:

`data/input.csv`

містить такі дані:

```text
Електролічильник-1;2026-09-01;125.50;4.32
Водомір-1;2026-09-02;12.75;35.20
Електролічильник-2;2026-09-03;48.00;4.32
Теплолічильник-1;2026-09-04;-10.00;8.50
Водомір-2;2026-09-05;abc;35.20
Електролічильник-3;;90.00;4.32
Газовий-лічильник;2026-09-07;33.00
```

Перші три записи є коректними.

Четвертий запис містить від’ємне значення `value`.

П’ятий запис містить текст `abc` замість числа.

Шостий запис містить порожнє поле `date`.

Сьомий запис містить три поля замість чотирьох.

### Звичайний запуск

```powershell
java -cp out ua.lpnu.kzp.Main
```

Приклад результату:

```text
Прочитано рядків: 7
Рядок 1: запис коректний
Рядок 2: запис коректний
Рядок 3: запис коректний
Рядок 4 пропущено: value не може бути від'ємним
Рядок 5 пропущено: value і tariff мають бути числами
Рядок 6 пропущено: поле date не може бути порожнім
Рядок 7 пропущено: очікується 4 поля

Результати:
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50
```

За замовчуванням звіт записується у файл:

`out/report.txt`

### Параметр `--help`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --help
```

Результат:

```text
Використання: java -cp out ua.lpnu.kzp.Main [--help] [--input <файл>] [--output <файл>]
```

### Параметр `--input`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --input data\input.csv
```

Параметр `--input` дозволяє передати шлях до вхідного файла
без зміни вихідного коду програми.

### Параметр `--output`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --output reports\result.txt
```

Параметр `--output` дозволяє визначити шлях до файла звіту.

### Одночасне використання параметрів

```powershell
java -cp out ua.lpnu.kzp.Main --input data\input.csv --output reports\result.txt
```

Файл звіту містить:

```text
Результати:
Кількість коректних записів: 3
Сумарне споживання: 186.25
Загальна вартість: 1198.32
Найбільше споживання: 125.50
```

Файл відкривається у VS Code у кодуванні UTF-8,
українські символи відображаються правильно.

---

## 8. Тестування

На першому рівні було виконано ручне тестування програми.

### Перевірка компіляції

Команда:

```powershell
javac -d out src\main\java\ua\lpnu\kzp\Main.java
```

Результат:

компіляція завершилася успішно.

### Перевірка звичайного запуску

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main
```

Перевірено:

- читання `data/input.csv`;
- обробку всіх рядків;
- продовження роботи після помилки в окремому записі;
- правильність чотирьох показників;
- формування файла звіту.

### Перевірка `--help`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --help
```

Результат:

програма вивела довідку та завершила роботу
без обробки CSV-файла.

### Перевірка `--input`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --input data\input.csv
```

Результат:

програма успішно прочитала явно переданий файл.

### Перевірка `--output`

Команда:

```powershell
java -cp out ua.lpnu.kzp.Main --input data\input.csv --output reports\result.txt
```

Результат:

створено файл:

`reports/result.txt`

Вміст файла збігається з підсумковим звітом у консолі.

### Перевірені помилкові дані

Було перевірено:

- від’ємне значення;
- нечислове значення;
- порожнє обов’язкове поле;
- неправильну кількість полів.

У кожному випадку програма повідомляє номер рядка
та причину його пропуску.

Хибні записи не впливають на підсумкові показники.

Також реалізована перевірка:

`validCount == 0`

Якщо коректних записів немає, програма повідомляє про це
та не формує некоректну статистику.

Автоматизовані JUnit-тести будуть додані на наступному рівні.

---

## 9. Документація

Для головного класу програми додано Javadoc.

Документовано:

- клас `Main`;
- приватний конструктор `Main()`;
- метод `main(String[] args)`.

Для методу `main` описано параметр:

`@param args аргументи командного рядка`

У `README.md` описано:

- номер лабораторної роботи;
- номер варіанта;
- предметну область;
- призначення програми;
- формат вхідного CSV;
- призначення полів;
- перелік обчислюваних показників;
- команду компіляції;
- звичайний запуск;
- `--help`;
- `--input`;
- `--output`;
- структуру проєкту.

---

## 10. Академічна доброчесність

Під час виконання лабораторної роботи використовувався
генеративний інструмент **ChatGPT від OpenAI**.

ChatGPT використовувався як допоміжний інструмент для:

- пояснення методичних вимог;
- планування послідовності виконання роботи;
- пояснення команд Git;
- пояснення конструкцій Java;
- перевірки коду;
- пошуку причин помилок компіляції;
- перевірки відповідності реалізації вимогам варіанта.

Весь програмний код був введений, запущений та перевірений
автором роботи.

Автор роботи може пояснити призначення використаних конструкцій
і логіку програми.

### Роль Manager

У файлі:

`ai/manager.md`

описана роль Manager.

Її призначення:

- аналізувати вимоги методички;
- розбивати роботу на послідовні етапи;
- контролювати виконання вимог поточного рівня;
- не переходити до наступного рівня до завершення попереднього.

Приклад промпту:

> Працюй лише за методичними вказівками.
> Розбий вимоги на послідовні кроки,
> пояснюй мету кожного кроку
> та перевіряй критерії готовності.

### Роль DevOps

У файлі:

`ai/devops.md`

описана роль DevOps.

Її призначення:

- допомога з Git і GitHub;
- Maven;
- Maven Wrapper;
- статичний аналіз;
- JAR;
- GitHub Actions.

Приклад промпту:

> Пояснюй кожен інфраструктурний крок
> та не додавай інструменти,
> які ще не потрібні для поточного рівня.

### Прийняті рекомендації

Під час роботи було прийнято рекомендації:

- виконувати лабораторну роботу у гілці `lab01`;
- використовувати `Path` для шляхів;
- використовувати явне UTF-8;
- використовувати `Locale.ROOT`;
- використовувати `split(";", -1)`;
- обробляти помилку одного рядка без завершення всієї програми;
- відхиляти від’ємні значення;
- перевіряти наявність хоча б одного коректного запису;
- додати `--help`, `--input` та `--output`;
- один раз формувати текст звіту
  і використовувати його для консолі та файла.

### Виправлені помилки

Під час розробки було виявлено помилку компіляції,
пов’язану з використанням:

`Locale.ROOT`

Компілятор повідомляв, що не знаходить `Locale`.

Причиною був відсутній імпорт:

```java
import java.util.Locale;
```

Після його додавання програма успішно скомпілювалася.

Також було виявлено дублювання перевірки аргументу:

`--help`

Один із повторних блоків було видалено,
а обробку аргументів залишено в одному циклі.

Під час перевірки українського тексту у Windows Terminal
спостерігалася проблема з відображенням окремих символів.

Було відкрито створений файл `reports/result.txt`.
У файлі українські символи були записані правильно,
що підтвердило коректність використання UTF-8 у програмі.

---

## 11. Відповіді на контрольні питання

### 1. Для чого потрібен файл `pom.xml`?

`pom.xml` описує Maven-проєкт: його параметри, версію Java,
залежності та плагіни.

На першому рівні Maven ще не використовується.
`pom.xml` буде додано на наступному рівні.

### 2. Для чого використовується метод `main`?

Метод `main` є точкою входу в консольну Java-програму.

JVM починає виконання програми з:

`public static void main(String[] args)`

### 3. Що містить `String[] args`?

Масив `args` містить аргументи,
які були передані програмі з командного рядка.

Наприклад:

```text
--input data\input.csv
```

буде представлено як:

```text
args[0] = "--input"
args[1] = "data\input.csv"
```

### 4. Для чого потрібен параметр `--help`?

`--help` виводить довідку про доступні параметри запуску програми.

### 5. Для чого потрібен параметр `--input`?

`--input` дозволяє вказати інший шлях до вхідного файла
без зміни вихідного коду.

### 6. Для чого потрібен параметр `--output`?

`--output` дозволяє вибрати шлях до файла,
в який буде записано сформований звіт.

### 7. Чому використовується `Path`?

`Path` дозволяє працювати зі шляхами
незалежно від конкретної операційної системи.

Це кросплатформніший підхід,
ніж ручне складання шляхів через `\` або `/`.

### 8. Навіщо явно задавати `StandardCharsets.UTF_8`?

Явне UTF-8 робить читання та запис тексту
незалежними від стандартного кодування операційної системи.

Це важливо для коректної роботи українських символів.

### 9. Для чого використовується `split(";", -1)`?

Метод `split` розділяє CSV-рядок на окремі поля.

Аргумент `-1` дозволяє зберігати порожні поля,
у тому числі порожнє поле в кінці рядка.

### 10. Що станеться при `Double.parseDouble("abc")`?

Виникне виняток:

`NumberFormatException`

У програмі ця помилка перехоплюється,
рядок пропускається,
а обробка наступних записів продовжується.

### 11. Чому неправильний рядок не можна мовчки пропускати?

Користувач повинен знати номер пошкодженого рядка
та причину помилки,
щоб мати можливість виправити вхідні дані.

### 12. Які перевірки виконуються для варіанта №9?

Програма перевіряє:

- чи рядок не порожній;
- чи є рівно 4 поля;
- чи `meter` не порожній;
- чи `date` не порожня;
- чи `value` є числом;
- чи `tariff` є числом;
- чи `value` не від’ємне;
- чи `tariff` не від’ємний;
- чи існує хоча б один коректний запис.

### 13. Чому некоректний запис не повинен змінювати статистику?

Якщо хибне значення додати до суми або врахувати при пошуку максимуму,
кінцевий звіт стане неправильним.

Тому статистика змінюється тільки після проходження всіх перевірок.

### 14. Для чого використовується `Locale.ROOT`?

`Locale.ROOT` дозволяє форматувати дробові числа
незалежно від регіональних налаштувань системи.

Наприклад:

`186.25`

буде записане з крапкою.

### 15. Для чого використовується `%n`?

`%n` додає правильний для поточної операційної системи
роздільник рядків.

Це кросплатформний спосіб перенесення рядка.

### 16. Для чого потрібен Git?

Git зберігає історію змін проєкту,
дозволяє використовувати гілки,
повертатися до попередніх станів
і об’єднувати зміни.

### 17. Навіщо лабораторна робота виконується в окремій гілці?

Окрема гілка дозволяє виконувати зміни,
не змінюючи стабільний стан `main`.

Після завершення роботу можна перевірити через Pull Request
і лише після code review об’єднати з `main`.

### 18. Для чого потрібен GitHub Issue?

GitHub Issue використовується для опису
окремого завдання, проблеми або етапу роботи.

У ньому можна записати мету,
критерії готовності та стан виконання.

### 19. Для чого потрібен агент DevOps?

DevOps допомагає з Git, Maven, Maven Wrapper,
статичним аналізом, JAR і GitHub Actions.

На першому рівні основна його роль була пов’язана
з налаштуванням Git і структури репозиторію.

### 20. Який рядок програми потребує додаткового пояснення?

Одним із найменш очевидних рядків є:

```java
input = Path.of(args[++i]);
```

Оператор `++i` спочатку збільшує значення індексу `i`.

Якщо поточним аргументом є:

`--input`

то наступним елементом масиву є шлях до файла.

Наприклад:

```text
args[0] = "--input"
args[1] = "data\input.csv"
```

Тому після `++i` програма отримує:

`args[1]`

і створює з нього об’єкт `Path`.

---

## 12. Висновки

У результаті виконання першого рівня лабораторної роботи
було створено консольну Java-програму
для предметної області «Комунальні показники».

Програма працює з варіантом №9
та обробляє записи формату:

`meter;date;value;tariff`

Було реалізовано перевірку вхідних даних,
обробку помилкових рядків
і продовження роботи після локальної помилки.

Для коректних записів програма обчислює:

- кількість коректних записів;
- сумарне споживання;
- загальну вартість;
- найбільше споживання.

Реалізовано параметри:

`--help`

`--input`

`--output`

Вхідні та вихідні файли обробляються у UTF-8.

Для кросплатформної роботи використовуються:

- `Path`;
- `Files`;
- `StandardCharsets.UTF_8`;
- `Locale.ROOT`;
- `%n`.

Створено публічний GitHub-репозиторій
та окрему гілку `lab01`.

Поточна реалізація першого рівня
стане основою для переходу до другого рівня лабораторної роботи,
де буде додано Maven, Maven Wrapper, автоматизовані JUnit-тести,
статичний аналіз та створення виконуваного JAR.